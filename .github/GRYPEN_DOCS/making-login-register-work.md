# Making the /login and /register work

1. Message GrypenBrigade for the ``.env.local`` file containing the necessary Firebase API keys
2. Drag the ``.env.local`` file to ``application/frontend``
3. Run ``npm run dev``
4. Register an account

