const networkRoles = {
    "networkAdmin": 5000,
    "member": 8080,
    "node_manager": 7050
}

module.exports = networkRoles;

// this is the roles that we will impose on users were invited or added by the admin.